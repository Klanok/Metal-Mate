{
  "name": "metal-mate",
  "private": true,
  "version": "0.0.0",
  "description": "Desktop 3D sheet metal modeller: unfold-to-flat, DXF export for laser cutting, PDF drawings.",
  "type": "module",
  "workspaces": [
    "core"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
