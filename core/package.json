{
  "name": "@metal-mate/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure domain library: feature model, face-bend graph, unfold engine, validation, DXF I/O. No DOM, no Tauri.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "clipper2-wasm": "0.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
